package arta.registrar.tutor.html;

import arta.registrar.tutor.logic.RegistrarDay;
import arta.registrar.tutor.logic.Registrar;
import arta.common.logic.util.Constants;

import java.util.Comparator;


public class RegistrarComparator implements Comparator {
    public int compare(Object o1, Object o2) {
        /*if (((RegistrarDay)o1).date.day>((RegistrarDay)o2).date.day)
            return 1;
        if (((RegistrarDay)o1).date.day<((RegistrarDay)o2).date.day)
            return -1;
        if (((RegistrarDay)o1).type > ((RegistrarDay)o2).type)
            return -1;
        if (((RegistrarDay)o1).type < ((RegistrarDay)o2).type)
            return 1;
        return 0;*/
        return 0;
    }
}
